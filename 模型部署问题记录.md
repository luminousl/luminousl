# 模型部署数值溢出
## 1.问题描述
在进行 ONNX 模型转换或推理初始化阶段，系统检测到模型内部的一个常量节点（Constant_18）包含数值 $100,000$。该数值触发了部署工具链的安全阈值限制（通常限制在 $10,000$ 以内）。
算法逻辑：该节点位于 Transformer 的 Cross-Attention（交叉注意力）模块中。为了实现 Mask（掩码）功能，算法通常会加上一个极大的负数，使 $Softmax$ 后的权重趋近于 0。硬件/精度限制：虽然训练时使用 FP32，但部署端（如车载 NPU/GPU）往往会使用 FP16 进行加速。FP16 的最大表达范围仅为 65,504。$100,000$ 超过了此上限，会导致物理层面的数值溢出（Overflow），使结果变为 Inf（无穷大）。量化约束：文件名显示该模型经过了 int8opt 优化。量化框架（如 TensorRT 或 SNPE）为了保证计算稳定性，通常不允许模型中存在量级悬殊的常量，否则会严重破坏量化比例因子（Scale）的精度。
## 2. 解决方案 (Solution)
针对此类数值溢出问题，核心思路是在保证算法逻辑有效的前提下，将数值压缩到硬件可接受的安全范围内。方案 A：手动修改 ONNX 常量（推荐，最快）由于注意力掩码只需要“足够小”即可生效，将 $-100,000$ 改为 $-10,000$ 在数学效果上几乎等价，且能完美避开 FP16 溢出点。


# 精度风险 (Accuracy Issues)

Accuracy.QDQ.ScaleOverflow / Underflow: 检查量化算子（Q/DQ）的缩放因子（Scale）。如果 Scale 太大（超过 1000）或太小（低于 1e-5），会导致量化后的数值精度严重丢失。
## 问题原因：
数值分布范围过大
## 解决方式：只对权重进行量化
1.激活值收输入影响大，容易产生离群值，一些算子softmax、layernorm对激活值极其敏感。
2.减少权重读取的内存带宽压力
3.硬件支持 英伟达对W8A16支持得更好

Function.QDQ.ScaleZero: 检查 Scale 是否为 0。Scale 为 0 会导致除零错误，使整个张量变成 NaN。
## 问题原因
1.激活值或者权重全为0
* 学习率设置不当，神经元死亡
* 过度剪枝，
* 数值范围太小
* 校准数据集不合适，全黑图片或者样本量过小

Function.FloatTensor.UnknownValue: 检查模型中是否存在没有赋值的常量或初始化器（Initializer）。

# 性能瓶颈 (Performance Issues)
这部分检查模型结构是否会导致推理速度变慢：

Performance.QDQ.ScaleMismatch: 检查相互连接的 Q/DQ 节点是否有不同的 Scale。如果 Scale 不匹配，推理引擎（如 TensorRT）无法进行算子融合，必须插入额外的重新量化操作，增加耗时。

Performance.UseGroupConv: 警告使用了组卷积（Group Convolution）。在某些硬件上，组卷积的效率远低于普通卷积。

Performance.PreBN.CannotBeFused: 检查 BatchNormalization 是否在卷积层之前。如果 BN 在卷积前且卷积带有 Padding，则无法进行“算子融合”，导致多余的计算开销。

Performance.QDQ.GridSampleInput...: 针对 GridSample 算子的专项检查。检查其输入是否为特定的 FP16 或卷积输出，以确保能触发硬件加速。

Performance.DanglingBN: 检查“悬空”的 BN 层（即后面没有紧跟卷积等可融合算子），这通常意味着结构设计不合理。

# 特定引擎 Bug 模式 (TRT Bug Patterns)
代码专门针对 TensorRT 10 版本避开了一些已知的架构坑：

TrtBugPattern.ConvTransposeTopk: 检查是否存在 Conv -> Transpose -> TopK 的模式。这种组合在某些 TensorRT 版本中存在解析 Bug，可能导致崩溃或结果错误。

TrtBugPattern.ConvSliceSigmoid: 检查 Conv -> Slice -> Sigmoid 模式。这同样是为了规避特定后端的优化 Bug。

# 算子渗透与子图检查 (QDQ Penetration)
same_scales_partition_iterative: 这是一个很高级的检查。它会自动搜索模型中哪些算子（如 Concat, Reshape, Transpose）被包裹在 Q/DQ 之间。它要求这些被包裹的算子两端的 Scale 必须完全一致，否则就会报 ScaleMismatch。